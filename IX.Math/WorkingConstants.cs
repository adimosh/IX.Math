using System;

namespace IX.Math
{
    internal static class WorkingConstants
    {
        internal static readonly Type defaultNumericType = typeof(int);
        internal static readonly Type defaultNumericTypeWithFinder = typeof(double);
    }
}